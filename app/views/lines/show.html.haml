%h1= 'Line ' +  @line_name
%table#stops
  %tr
    %th Location
    %th Time
    - @stops.each do |stop|
      %tr{:class => cycle('odd', 'even')}
        %td= link_to Location.find(stop.location_id).name, location_path(stop.location_id)
        %td= Time.parse(stop.hour.to_s + ':' + stop.minute.to_s).strftime("%l:%M %p")
